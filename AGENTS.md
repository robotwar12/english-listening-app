# 프로젝트 작업 지침

## 개요와 읽기 순서
- Next.js App Router + React + TypeScript 기반 영어 단어 듣기 앱. npm 사용.
- 현재 화면은 VOCA 3200 자료의 1~750번 범위를 무작위로 섞어 수동 재생한다.
- 기능 수정 시 `docs/code-map.md`에서 관련 위치를 찾고 해당 소스부터 읽는다.
- 실행·검증 절차는 `docs/development.md`, 사용자용 개요는 `README.md`를 필요할 때 읽는다. 매번 모든 문서를 읽지 않는다.

## 탐색과 토큰 사용
- 먼저 `git status --short`로 기존 변경을 확인한다. 사용자 변경을 덮어쓰거나 되돌리지 않는다.
- 파일명·심볼을 검색한 다음 필요한 범위만 읽는다. 호출부·타입·설정은 필요에 따라 탐색을 확장한다.
- `rg`가 있으면 우선 사용한다. 없으면 `git grep -n`, `git ls-files`, `find src -type f`, `sed -n`을 사용한다.
- 일반 탐색에서 `node_modules`, `.next`, `.git`, 빌드 결과물, `*.tsbuildinfo`, 생성된 `public/sw.js`, `public/workbox-*.js`를 제외한다.
- `public/audio*` 전체 파일명이나 대용량 단어 사전을 출력하지 않는다. 개수·일치 여부·필요한 항목만 조회한다.
- lock 파일은 의존성 문제를 다룰 때만 확인한다. 이미 읽은 변경 없는 파일의 반복 읽기를 피한다.
- 문서는 탐색 안내이며 실제 코드가 기준이다. 불일치가 있으면 코드 확인 후 해당 문서를 갱신한다.

## 수정 원칙
- 학습 UI와 상태·오디오 제어의 진입점은 `src/components/english-listening-app.tsx`다.
- 현재 사용 데이터는 `/api/files` → `public/audio3`, `/voca3200data.json`이다. 이전 데이터는 요청 없이 삭제·재연결하지 않는다.
- 오디오 파일명, 번호 범위, 뜻 데이터 키의 연결을 유지한다. 자세한 계약은 코드 지도에 있다.
- `audioRef`와 `isPlaying` 상태를 함께 확인한다. 다음 곡 자동 재생·종료 후 자동 이동은 현재 동작이 아니므로 변경 시 의도를 명확히 한다.
- 기존 TypeScript 타입, `@/` 경로 별칭, UI 컴포넌트를 활용한다. 요청과 무관한 재구성·패키지 변경은 피한다.
- PWA 생성 파일을 직접 수정하지 않는다. 빌드 후 생성 파일의 Git 변경을 확인한다.

## 완료 기준
- 코드 변경 시 관련 검증을 실행한다. 기본 정적 검사는 `npx tsc --noEmit --incremental false`와 `npx eslint src`다.
- UI·재생 변경은 관련 브라우저 동작도 확인한다. 빌드·PWA 변경은 개발 모드만으로 검증 완료라 하지 않는다.
- 문서만 변경했으면 경로·설명·diff를 검증하며 불필요한 빌드를 실행하지 않는다.
- 기능 위치·데이터 흐름 변경은 `docs/code-map.md`, 실행·검증 방법 변경은 `docs/development.md`의 해당 항목만 갱신한다. 작업 일지를 계속 누적하지 않는다.
- 완료 보고는 변경 내용, 실행한 검증과 결과, 미검증 사항·관련 기존 문제를 짧게 남긴다.
